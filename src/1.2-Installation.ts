// Initialize directory with it creates package.json file for us
// npm init -y

// Install package typescript
// npm install typescript --save-dev

// Provide the configuration file to tell typescript how to behave
// This creates a new typescript config file in the current dir
// npx tsc --init

// Whatever you create in script in package.json you can then execute the script by name
// npm run dev (if I created a script with the name "dev")

// bun create vite my-ts-app --template vanilla-ts
// cd my-ts-app
// bun install
// bun run dev

// Run this command to compile files into js for production
// bun run build
